import React from 'react';
import useTitle from '../../../hooks/useTitle';

const MyReviews = () => {
    useTitle('My Reviews');

    return (
        <div>
            MyReviews
        </div>
    );
};

export default MyReviews;